<%= form_for @invite, url: group_invites_path(@invite.group) do |f| %>
  <%= f.hidden_field :group_id, value: @invite.group_id%>
  <%= f.label :email%>
  <%= f.email_field :email, placeholder: 'example@example.com' %>
  <%= f.submit 'Send an invite' %>
<% end %>
